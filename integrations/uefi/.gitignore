# U-1 UEFI shim build/run artifacts (regenerated by build.sh / run.sh)
BOOTX64.EFI
BOOTX64.lib
*.obj
*.o
*.so
esp.img
vars_rw.fd
# freestanding archive is built in the repo root, not here, but guard anyway
libkeel_freestanding*.a
# U-2 harness artifacts
wwwroot/
